claude --permission-mode bypassPermissions



 你要进入「修改-测试」循环模式，严格遵守：
【任务】评审上面的修改内容
【测试方式】用 dev-browser 打开浏览器（可视化）连接
  http://localhost:5173
   操作步骤：登岛(点哥哥) → 看岛 → ...（你要点的路径）    
    dev server 用 `npm run dev` 在后台跑
  【循环规则】
    1. 改代码
    2. 用 dev-browser 重新加载页面，执行操作步骤
    3. 截图 + 用 page.evaluate 取关键 DOM/数值
    4. 对比【成功标准】
       - 达标 → 把截图路径发我，停止
       - 不达标 → 回到第1步继续改，直到达标

  【成功标准】屏幕上要显示 XXX，数值等于 XXX
  【失败标准】如果还是 XXX 就继续修，不要问我

  每次循环结束告诉我：改了什么、测了什么、通过没。
